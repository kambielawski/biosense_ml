# Gaze-Based Organoid Video Prediction — Progress

## Step 1: Crop Extraction Pipeline [DONE]
- Created `biosense_ml/pipeline/gaze.py` — motion detection (frame differencing + Gaussian blur + center-of-mass), crop extraction, ImageNet normalization
- Created `scripts/build_gaze_dataset.py` — CLI entry point with parallel batch processing, HDF5 output with per-batch groups (crops, centers, deltas, timestamps, has_motion, split)
- Created `slurm/build_gaze_dataset.sh` — Slurm submission script (CPU, short partition, 8 workers)
- Created `tests/test_gaze.py` — 13 tests covering motion detection, crop extraction, normalization, delta consistency
- All 22 tests pass (13 new + 9 existing)
- Uses same train/val/test split logic as trajectory pipeline (assign_splits, seed=42, stratified)

## Step 2: Dynamics Model [DONE]
- Created `biosense_ml/models/gaze_dynamics.py` — Conv Encoder (3→32→64→128) + MLP Predictor (K×128→256→130) + Conv Decoder (128→64→32→3), ~813K params at full size
- Created `biosense_ml/pipeline/gaze_dataset.py` — GazeCropDataset (sliding windows, motion-only targets) + GazeSequenceDataset (full sequences for eval)
- Created `configs/model/gaze_dynamics.yaml` — latent_dim=128, hidden_dim=256, context_len=10
- Created `configs/training/gaze.yaml` — AdamW lr=1e-4, cosine schedule, L1 crop + L1 delta loss, lambda_pos=1.0
- Created `scripts/train_gaze.py` — training script with baseline comparison (copy-last-crop, zero-delta), W&B logging, checkpointing
- Created `slurm/train_gaze.sh` — Slurm submission script (nvgpu partition, 1 GPU, 24h)
- Registered `gaze_dynamics` in `biosense_ml/models/__init__.py` MODEL_REGISTRY
- Created `tests/test_gaze_dynamics.py` — 20 tests covering model shapes, gradients, param count, dataset loading, dtypes, integration, single-sample overfit
- All 42 tests pass (20 new + 22 existing)
## Step 3: Evaluation [DONE]
- Created `biosense_ml/evaluation/gaze_eval.py` — GazeRolloutMetrics dataclass, evaluate_rollout() (crop L1, baseline crop L1, ADE/FDE/HDE via cumulative deltas, velocity error, divergence detection), evaluate_dataset() (aggregated stats with median/mean/IQR, divergence rate, crop improvement %)
- Created `scripts/eval_gaze.py` — CLI eval entry point with checkpoint loading, filmstrip generation (GT vs predicted crops side-by-side), trajectory overlay plots, W&B logging with eval-gaze-v1 tag
- Created `slurm/eval_gaze.sh` — Slurm submission script (gpu-debug partition, 15min)
- Created `tests/test_gaze_eval.py` — 18 tests covering metrics dataclass, rollout evaluation (shapes, short sequences, HDE horizons, velocity error, crop L1), dataset evaluation (aggregation, empty/short inputs, IQR format, divergence rate, length-weighted ADE), integration (H5 loading, checkpoint round-trip)
- All 60 tests pass (18 new + 42 existing)

## Step 4: Full-Frame Compositing [DONE]
- Created `biosense_ml/pipeline/compositing.py` — build_alpha_mask (4px cosine falloff), denormalize_crop (ImageNet→RGB uint8), composite_crop_on_frame (feathered alpha blend), load_background_frame (from raw archive), generate_rollout_video (MP4 via cv2.VideoWriter with optional GT side-by-side)
- Created `scripts/generate_gaze_rollout.py` — CLI entry point with checkpoint loading, model rollout, archive background loading (with dark-gray fallback), side-by-side GT vs predicted videos, per-sequence metrics logging, W&B integration with video artifacts
- Created `slurm/generate_gaze_rollout.sh` — Slurm submission script (gpu-debug partition, 15min, node exclusions)
- Created `tests/test_compositing.py` — 24 tests covering alpha mask (shape, symmetry, edge=0, center=1, monotonic, custom size), denormalize (shape, dtype, roundtrip, clipping), compositing (blend, clamping, background preservation, feathering), video generation (file creation, side-by-side, nested dirs)
- All 84 tests pass (24 new + 60 existing)
