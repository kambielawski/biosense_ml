name: trajectory_mlp

# Feature dimensions
input_dim: 10       # centroid_xy(2) + velocity_xy(2) + dt(1) + actions(5)
output_dim: 2       # predicted (x, y) position
context_len: 10     # K — number of past timesteps to use

# Architecture
hidden_dim: 64
num_layers: 2
dropout: 0.0
